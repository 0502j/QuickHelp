const SET_MODAL = (state, obj) => {
    state.modal = obj
}

const SET_LIST = (state, obj) => {
    state.list = obj
}

const SET_EVIDENCES = (state, obj) => {
    state.list = obj
}

const SET_DIAGNOSTIC = (state, obj) => {
    state.diagnostic = obj
}

SET_DIAGNOSTIC

export default {
    SET_MODAL,
    SET_LIST,
    SET_EVIDENCES,
    SET_DIAGNOSTIC
}